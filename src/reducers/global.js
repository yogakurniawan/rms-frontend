const initialState = {
  dummyGlobalState: false
};

const globalReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default globalReducer;
